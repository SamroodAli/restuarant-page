import {header,nav,ul,li,a} from './tags'


const navLinks = [
  li(a(), "")
]

const pageHedaer = () => {
  return header(
    [
      nav(
        ul(
          
        )
      )
    ],"bg-primary"
  )
}
